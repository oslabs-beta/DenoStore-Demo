import * as React from 'react';
import Homepage from './Homepage';
import '../styles.css';

//do we need to type what it returns?
const App: React.FC = () => {
  return (
    <div>
      <Homepage />
    </div>
  );
};

export default App;
